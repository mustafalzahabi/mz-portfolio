# Portfolio (Twitter-like)

This is a small portfolio site implemented with pure HTML, CSS and JavaScript. All content is loaded from `data.json`.

How to run locally:

```powershell
# from project root
python -m http.server 8000
# then open http://localhost:8000 in your browser
```

Files:
- `index.html` – main page
- `style.css` – styles
- `script.js` – loads `data.json` and renders content
- `data.json` – your portfolio data
- `assets/` – images (replace placeholders)
